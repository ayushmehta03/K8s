kubectl expose service prometheus-server \
  --type=NodePort \
  --target-port=9090 \
  --name=prometheus-server-ext


   minikube service prometheus-server-ext --url



helm repo add grafana https://grafana.github.io/helm-charts
helm repo update


helm install my-grafana grafana/grafana --create-namespace -n monitoring


kubectl get secret --namespace default  my-grafana -o jsonpath="{.data.admin-password}" | base64 --decode ; echo

kubectl get svc -n monitoring


 # kubectl patch svc my-grafana -n monitoring -p '{"spec": {"type": "NodePort"}}

  # minikube service my-grafana -n monitoring



